/*
This page will get the document-id from the params and fetch all the ocr jobs for that document.
It will also fetch the document details from the database and display them on the page.
It will also allow the user to create a new ocr job for that document.
It will also allow the user to delete an ocr job for that document.

*/